/* GLOBAL INTERFACES
   ========================================================================== */

export interface StatusOptions {
  value: string;
  label: string;
}

export interface IAppInfo {
  name: string;
  version: string;
}

export type TAppInfo = IAppInfo;
